--- rest26/tutorial/stdlib.rst	2008-12-19 16:58:09.000000000 +0900
+++ rest262/tutorial/stdlib.rst	2009-04-05 05:01:50.000000000 +0900
@@ -127,25 +127,25 @@
    10.0
 
 The :mod:`random` module provides tools for making random selections::
 
    >>> import random
    >>> random.choice(['apple', 'pear', 'banana'])
    'apple'
    >>> random.sample(xrange(100), 10)   # sampling without replacement
    [30, 83, 16, 4, 8, 81, 41, 50, 18, 33]
    >>> random.random()    # random float
    0.17970987693706186
    >>> random.randrange(6)    # random integer chosen from range(6)
-   4   
+   4
 
 
 .. _tut-internet-access:
 
 Internet Access
 ===============
 
 There are a number of modules for accessing the internet and processing internet
 protocols. Two of the simplest are :mod:`urllib2` for retrieving data from urls
 and :mod:`smtplib` for sending mail::
 
    >>> import urllib2
